# Cab Booking — raise body limit for API JSON (driver onboarding: base64 images).
# Loaded from /etc/nginx/conf.d/ via default nginx.conf (http { ... }).
# Without this, Nginx default is 1m → large PATCH/POST fail; browsers often show "Network Error".
client_max_body_size 25m;
