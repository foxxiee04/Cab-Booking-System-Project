$kind: http-request
url: "{{docker_gateway_url}}/api/auth/register-phone/verify"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "phone": "{{customerPhone}}",
      "otp": "{{otp}}"
    }
auth:
  type: noauth
order: 3000
