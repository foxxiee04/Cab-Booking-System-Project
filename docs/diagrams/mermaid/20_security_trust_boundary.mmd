%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#f8fafc', 'primaryTextColor': '#111827', 'primaryBorderColor': '#64748b', 'lineColor': '#334155'}}}%%
flowchart TB
  subgraph PUB["Public internet"]
    C[Clients]
  end

  subgraph DMZ["Edge trust"]
    LB[TLS termination]
  end

  subgraph APP["Application trust"]
    GW[Gateway services]
  end

  subgraph DATAZ["Data trust"]
    DB[(Databases)]
    SEC[Secrets vault]
  end

  C -->|TLS only| LB --> GW
  GW -->|scoped creds| DB
  GW --> SEC
